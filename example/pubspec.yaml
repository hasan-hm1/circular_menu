name: circular_menu_example
version: 0.1.0

publish_to: 'none'

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  circular_menu:
    path: ../

flutter:
  uses-material-design: true
